node_modules
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev/dev.sqlite
/prisma/dev/dev.sqlite-journal
database.sqlite

.env
.env.*

yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Minified build output of widget-src/*.js — see "build:widget-js" in
# package.json, always run before `shopify app deploy`. Source of truth
# lives in /widget-src (kept outside extensions/magyx-bundle-widget since
# theme app extensions only allow assets/blocks/locales/snippets at their
# top level); these are regenerated every deploy.
/extensions/magyx-bundle-widget/assets/*.js

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock
